<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="110dp"
    android:layout_height="wrap_content"
    android:id="@+id/cl_container_itemcubierta"
    android:padding="16dp">

    <ImageView
        android:id="@+id/iv_img_itemcubierta"
        android:layout_width="35dp"
        android:layout_height="35dp"
        android:backgroundTint="#D2BACF"
        app:layout_constraintEnd_toEndOf="@+id/tv_nombre_itemcubierta"
        app:layout_constraintStart_toStartOf="@+id/tv_nombre_itemcubierta"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_cubierta" />

    <CheckedTextView
        android:id="@+id/tv_nombre_itemcubierta"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="TextView"
        android:checked="false"
        android:textAlignment="center"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/iv_img_itemcubierta" />

</androidx.constraintlayout.widget.ConstraintLayout>